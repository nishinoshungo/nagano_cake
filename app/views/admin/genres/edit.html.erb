<h4 class="m-5">ジャンル編集</h4>
<div class="mt-5 mb-5">
  <%= form_with model: @genre, url: "/admin/genres/#{@genre.id}", local:true do |f| %>
    <label>ジャンル名</label>
    <%= f.text_field :name, class: "mx-3" %>
    <%= f.submit "変更を保存", class: "btn btn-success"%>
  <% end %>
</div>